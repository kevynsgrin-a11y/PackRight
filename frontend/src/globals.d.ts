/**
 * Build-time globals substituted by vite.config.ts `define`.
 *
 * `vite/client` types come from tsconfig.app.json's `types` array, so this file
 * carries only PackRight's own declarations.
 */

/** The four-digit year the bundle was built, so SSR and the client cannot disagree. */
declare const __BUILD_YEAR__: string
